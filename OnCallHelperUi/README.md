# OnCallHelperUi (retired)

This folder is intentionally retired to avoid confusion.

The active Vue CLI app lives in:

- `OnCallHelperUiCli/`

Use the commands there:

```bash
cd OnCallHelperUiCli
npm install
npm run serve
```
