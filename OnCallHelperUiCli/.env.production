# Used automatically by `npm run build` (the Docker image build).
# Local `npm run serve` still uses .env (localhost). Values here are public.
VUE_APP_AUTH0_DOMAIN=dev-k0sl1xaa1o87ofbn.us.auth0.com
VUE_APP_AUTH0_CLIENT_ID=lGRFsCzIrietbIxH1sNSQbTI0Uz9VoBU
VUE_APP_AUTH0_AUDIENCE=http://localhost:5172

# Deployed API on the custom CloudFront domain (VPN-friendly hostname).
# Rebuild/repush the UI image only AFTER api.oncallhelper.com is live in DNS.
VUE_APP_API_BASE_URL=https://api.oncallhelper.com
